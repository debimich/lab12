<table border="1">
  <tr>
    <th>Итерация</th>
    <th>Результат вычисления</th>
  </tr>

  <% @guesses.each_with_index do |guess, index| %>
    <tr>
      <td><%= index == 0 ? "Ввод" : index %></td>
      <td id="value_<%= index %>"><%= guess %></td>
    </tr>
  <% end %>
</table>

<%= link_to "Повторить вычисления", :sqrt_input%>